[package]
name = "speed_token"
version = "0.1.0"
edition = "2024"

[lib]
name = "speed_token"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.23", features = ["extension-module"] }
rayon = "1.10"
libc = "0.2"

[build-dependencies]
cc = "1.0"
